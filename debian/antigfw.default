# Defaults for temp initscript
# sourced by /etc/init.d/antigfw
# installed at /etc/default/antigfw by the maintainer scripts

RUNDAEMON=0
LOGFILE='/var/log/antigfw.log'
CONFIGFILE='/etc/antigfw/antigfw.conf'

# Additional options that are passed to the Daemon.
DAEMON_OPTS="-c $CONFIGFILE -l $LOGFILE"

